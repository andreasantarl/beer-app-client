{{#profile}}
  <form class="profile-display">
    <fieldset class="form-group">
      <div class="row">
        {{> edit_my_profile}}
      </div>
      <div class="profileEditButton">
      <input type="submit" class="edit-profile btn btn-default" id="{{@index}}" data-id="{{id}}" value="Update">
    </div>
    </fieldset>
  </form>
{{/profile}}
